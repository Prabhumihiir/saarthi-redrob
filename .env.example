# Anthropic API key for the Companion chat route (server-side only — never expose to the client).
# Get one at https://console.anthropic.com/ . Copy this file to .env.local and fill in the value.
ANTHROPIC_API_KEY=
