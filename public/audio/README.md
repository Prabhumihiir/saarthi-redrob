# Placeholder audio

These are short, gentle sine-tone placeholders so the app's `<audio>` elements
never 404 and the players/timers work end-to-end during the POC.

| File | Used by |
| --- | --- |
| `chant-placeholder.wav` | shlokas + aarti (Practice, Today) |
| `meditation-placeholder.wav` | guided meditations (Meditate) — looped under a data-driven timer |
| `katha-placeholder.wav` | puranic stories (Learn → Listen) |
| `japa-loop.wav` | japa chant loop (Chant) — seamless 2s loop |
| `bg-tanpura.wav` | session player background — tanpura drone |
| `bg-bells.wav` | session player background — temple bells |
| `bg-water.wav` | session player background — flowing water |

**TODO: replace with produced audio** (recorded chants, guided meditations,
katha narration, and ultimately a produced "deity voice").
